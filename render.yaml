services:
  - type: web
    name: vlad-bingo-web
    env: python
    buildCommand: "./backend/build.sh"
    # Added 'backend.' prefix here
    startCommand: "cd backend && python -m daphne vlad_bingo.asgi:application"
    envVars:
      - key: REDIS_URL
        fromService:
          type: redis
          name: vlad-redis
          property: connectionString
      - key: DATABASE_URL
        fromDatabase:
          name: vlad_db
          property: connectionString

  - type: worker
    name: vlad-bingo-bot
    env: python
    # Added 'cd backend' to ensure imports work
    startCommand: "cd backend && python bingo/bot/main.py"

  - type: redis
    name: vlad-redis
    plan: free
    ipAllowList: []

databases:
  - name: vlad_db
    plan: free
    ipAllowList: []
